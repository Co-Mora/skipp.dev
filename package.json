{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "scripts": {
    "start": "lerna exec --scope @workspace/pokemon-app -- npm run dev",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^6.4.1"
  }
}
